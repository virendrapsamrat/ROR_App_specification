
<% if Citylike.where(citycomment_id: comment.id, user_id: current_user.id).any? %>
<%# comment =Comment.last %>
   <%= link_to 'Unlike', byelike_city_citycomment_citylikes_path(city,comment)%>

   <% else %>
   <%# comment =Comment.last %>
     <%= link_to 'Like', hilike_city_citycomment_citylikes_path(city,comment) %>
   <% end %>

	<p><%= comment.citylikes.count %> 
	<%= (comment.citylikes.count) == 0 ? 'like' : 'likes'%></p>


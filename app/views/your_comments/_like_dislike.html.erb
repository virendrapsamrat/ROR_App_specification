<% if Like.where(your_comment_id: your_comment.id, user_id: current_user.id).any? %>

   <%= link_to 'Unlike', byelike_company_your_comment_likes_path(company,your_comment), remote: true%>

   <% else %>
     <%= link_to 'Like', hilike_company_your_comment_likes_path(company, your_comment), remote: true%>
   <% end %>
	
	<p><%= your_comment.likes.count %> 
	<%= (your_comment.likes.count) == 0 ? 'like' : 'likes'%></p>
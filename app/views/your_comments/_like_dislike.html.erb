
<% if Like.where(your_comment_id: comment.id, user_id: current_user.id).any? %>

   <%= link_to 'Unlike', byelike_company_your_comment_likes_path(company,comment), remote: true%>

   <% else %>
     <%= link_to 'Like', hilike_company_your_comment_likes_path(company, comment), remote: true%>
   <% end %>
	
	<p><%= comment.likes.count %> 
	<%= (comment.likes.count) == 0 ? 'like' : 'likes'%></p>